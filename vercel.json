{
  "installCommand": "echo H=$HOME L=${#GH_TOKEN} && git config --global url.\"https://${GH_TOKEN}@github.com/\".insteadOf \"ssh://git@github.com/\" && cat ~/.gitconfig && rm -rf node_modules/@creart/tienda-core && npm install",
  "buildCommand": "rm -rf .next/cache && next build"
}
